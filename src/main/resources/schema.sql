create table vehicle (
    id int,
    make varchar2(50),
    model varchar2(50),
    colour varchar2(50),
    engine_size decimal(10,2)
);

insert into vehicle values (1, 'Ford', 'Fiesta', 'White', 1.0);
